<ul className="sidebar-list">
          {contacts.map((c) => (
            <ContactItem
              key={c.userId}
              contact={c}
              active={currentPeer === c.userId}
              onClick={() => {
                setPeer(c.userId);
                onSelect?.(c.userId);
              }}
            />
          ))}
        </ul>